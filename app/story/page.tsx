import InnerLayout from "@/components/InnerLayout";
import StoryContent from "@/components/pages/StoryContent";

export const metadata = { title: "Our Story — VUDU Energy" };

export default function StoryPage() {
  return (
    <InnerLayout>
      <StoryContent />
    </InnerLayout>
  );
}
